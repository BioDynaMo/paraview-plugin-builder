=== How to build ParaView nightly image ===
 * Notes :
 * The superbuild will always build its last master and configure it with the last version of the configuration
 * This script is only currently only comptatible with ParaView 5.7.X nightlies.
 *
 * Usage :
 * ./run_build_paraview.sh nightly_hash
 * eg: ./run_build_paraview.sh 8f65d9a
 * go into plugin directory to build your plugin
